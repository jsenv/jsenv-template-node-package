/**
 *
 * When node.js executes code with
 *
 * import { DEV } from "#env"
 *
 * It will replace "#env" by this file by default.
 * Is it configured in the package.json "imports" field.
 *
 */

export const DEV = true
